{
  "log": { "loglevel": "warning" },
  "inbounds": [{
    "tag": "socks-in",
    "port": 10808,
    "listen": "127.0.0.1",
    "protocol": "socks",
    "settings": {
      "auth": "password",
      "accounts": [{ "user": "routex", "pass": "<GANTI_PASSWORD_INI>" }],
      "udp": true
    }
  }, {
    "tag": "http-in",
    "port": 10809,
    "listen": "127.0.0.1",
    "protocol": "http",
    "settings": {
      "accounts": [{ "user": "routex", "pass": "<GANTI_PASSWORD_INI>" }],
      "allowTransparent": false
    }
  }],
  "outbounds": [{
    "protocol": "freedom",
    "settings": {}
  }]
}
